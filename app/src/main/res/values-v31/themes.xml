<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Splash 화면의 배경색 지정 -->
        <item name="android:windowSplashScreenBackground">@color/LightPurple</item>
        <!-- Splash 화면의 중앙에 위치할 아이콘 Drawable 지정 -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/icon_main</item>
        <!-- Splash 화면이 나타나는 시간 지정 -->
        <item name="android:windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.HTap</item>
    </style>
</resources>